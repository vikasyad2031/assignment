import { TotalIntDirective } from './total-int.directive';

describe('TotalIntDirective', () => {
  it('should create an instance', () => {
    const directive = new TotalIntDirective();
    expect(directive).toBeTruthy();
  });
});
